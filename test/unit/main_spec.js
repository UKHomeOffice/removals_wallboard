/*global Angular */
describe('UNIT main.js', function() {

  xit('sets the requires array with the right elements.', function(){

  });

  xit('should mount the app on window', function () {
  });

  xit('should call constant() with the right parameters.', function () {
  });

  xit('should call config() with the right parameters.', function () {
  });

  xit('should call run, passing onRun', function () {
  });

  xit('should call bootstrap with the right parameters', function () {
  });

});
describe('UNIT: indexDirectives', function () {

  xit('should exist', function () {
  });

  describe('declare directives', function () {

    xit('should do nothing if item is falsey', function () {
    });

    xit('should add directive to directivesModule item has a fn method', function () {
    });

    xit('should recurse to next level if item does not have the relevant properties', function () {

    });

  });
});